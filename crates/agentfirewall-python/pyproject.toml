[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "agentfirewall"
requires-python = ">=3.9"
description = "Python SDK for Agent FirewallKit - guardrails for autonomous AI agents"
license = {text = "MIT"}

[tool.maturin]
features = ["pyo3/extension-module"]
